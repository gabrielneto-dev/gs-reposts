# frontend

Ainda não iniciado.

Vai ser a aplicação de interface com o usuário final, com **banco de dados próprio** (é o sistema
de registro de fato do projeto — diferente do `../backend/`, que é só um adapter sem persistência
pra API do NextRouter). Consome `../backend/` via HTTP para os dados do softswitch (ASR, ACD, PDD,
clientes) — ver `../backend/docs/API.md`.

Stack/framework ainda não definido.
