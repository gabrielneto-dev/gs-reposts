# O frontend não tem banco próprio (Prisma removido) — todos os dados vêm da API do backend/
# (FastAPI). Essa URL só é usada em Server Components/Route Handlers (nunca chega ao navegador).

BACKEND_API_URL=http://127.0.0.1:8000
